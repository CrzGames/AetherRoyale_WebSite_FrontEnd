apiVersion: v1
kind: Service
metadata:
  name: prod-aetherroyale-frontend-service
  namespace: prod-aetherroyale-frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: prod-aetherroyale-frontend
